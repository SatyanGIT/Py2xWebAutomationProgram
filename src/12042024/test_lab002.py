# Selenium
# Automates Browsers
# Selenium 3 - 20% on Selenium 3 - Json wire protocol
# Selenium 4 - 70% + migrated to Selenium 4 -> W3C Protocol, Selenium Manager

# pip install selenium -> 4.x -> you don't have to set up the browser drivers.
# Selenium 3
from selenium import webdriver

def test_open_website():
    pass
    # # driver_path = "/Program Files/msedge/msedgedriver"
    # # driver = webdriver.EdgeService(executable_path=driver_path)
    # driver.get("https://app.vwo.com")